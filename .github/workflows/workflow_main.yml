name: SonarQube Analysis

on:
  push:
  
jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          npm install
          npm install -D webpack-cli

      - name: Build and test
        run: |
          npm run build
          npm test
          npm run coverage
          
      - name: SonarQube Analysis
        uses: sonarsource/sonarcloud-github-action@v1
        with:
          organization: "serverless"
          token: sqp_44596f7ea84deb602b4618486b1ed3572e55b9af 
          projectBaseDir: /home/nidhii/sonar-scanner-4.8.0.2856-linux/bin
          projectKey: "serverless1"
          projectName: "serverless1"
          scannerMode: "CLI"
          extraProperties: |
      
