name: SonarQube Analysis

on:
  push:
  
jobs:
  sonarqube:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          npm install
          npm install -D webpack-cli

      - name: Build and test
        run: |
          npm run build
          npm test
          npm run coverage
          
      - name: Install SonarQube Scanner
        run: |
          npm install -g sonarqube-scanner

      - name: Run SonarQube analysis
        env:
          SONAR_RUNNER_HOME: "SonarQube Scanner 3.0.3"
        run: |
          ${SONAR_RUNNER_HOME}/home/nidhii/sonar-scanner-4.8.0.2856-linux/conf
          sonar-scanner -Dsonar.login="sqp_44596f7ea84deb602b4618486b1ed3572e55b9af"  
          sonar-scanner -Dsonar.projectKey="serverless1"
